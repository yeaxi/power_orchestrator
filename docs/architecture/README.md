# Power Orchestrator architecture references

`power-orchestrator-spec.md` is a project-local copy of the formerly global reference. This directory contains project-specific architecture context for the reactive/declarative orchestrator.

The active project policy is the repository-root `AGENTS.md`. The specification does not authorize live cutover, Home Assistant service calls, or physical actions. Use Git review and rollback before any approved deployment.

The original global reference remains intact for rollback/history.
